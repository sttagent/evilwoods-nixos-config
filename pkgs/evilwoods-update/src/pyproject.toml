[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
evilwoods-update = "evilwoods_update.cli:main"

[project]
name = "evilwoods-update"
version = "0.1.0"
dependencies = ["requests"]
